enable-magisk-update = true # set this to false if you do not want to receive updates for the module in magisk app
parallel-jobs = 1

# add 'enabled = false' for not patching a specific app or remove it from the config
# see https://github.com/j-hc/revanced-magisk-module/blob/main/CONFIG.md for more detailed explanations

# you can use rvmm-config-gen to generate a config

["Google Photos"]
apkmirror-dlurl = "https://apkmirror.com/apk/google-inc/photos/"
arch = "arm64-v8a"
build-mode = "module"
included-patches = "'Enable DCIM folders backup control'"
excluded-patches = "'GmsCore support'"
patcher-args = "-e 'Spoof features' -OfeaturesToEnable=com.google.android.apps.photos.NEXUS_PRELOAD, com.google.android.apps.photos.nexus_preload"
